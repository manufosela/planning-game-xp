{
  "name": "planning-game-mcp",
  "version": "1.23.0",
  "description": "Integrated MCP Server for Planning GameXP",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build:standalone": "node ../scripts/build-mcp-standalone.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "prepublishOnly": "node -e \"console.error('\\n  ERROR: Do not publish from mcp/ directly.\\n  The package published to npm must be the standalone build.\\n  Run from repo root:\\n    npm run mcp:publish\\n'); process.exit(1)\""
  },
  "author": "Geniova",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "firebase-admin": "^12.6.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
