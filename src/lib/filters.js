/**
 * Pure filter functions — re-exports from @pgv2/domain.
 * @module lib/filters
 */

export {
  getStatusesForType,
  matchesSearch,
  matchesTags,
  applyFilters,
} from '../../packages/domain/services/filters.js';
